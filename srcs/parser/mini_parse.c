#include "minishell.h"
#include "parser.h"

int	mini_parse(t_token_list *token_head, t_parsed_list **parsed_head)
{
	int	ret_value;

	ret_value = 0; ///////////// temp
	(void) token_head; ///////////// temp
	(void) parsed_head; ///////////// temp
	return (ret_value);
}
